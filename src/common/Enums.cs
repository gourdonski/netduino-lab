using System;
using Microsoft.SPOT;

namespace NetduinoLab.Common
{
    public enum Severity
    {
        Debug,
        Info,
        Warn,
        Error
    }

    public enum TimeZone
    {
        Utc,
        MountainTime
    }
}
